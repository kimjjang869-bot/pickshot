# PickShot v8.9 메모리 최적화 — 세션 핸드오프

**날짜**: 2026-04-22
**결과**: RSS 18GB → 221MB 피크 (98.8% 감소)
**다음 세션**: Phase 3/4/5 + 커밋/배포 결정

## ✅ 완료 (Phase 1+2)

### CG raster 해소 — 최대 효과
1. `kCGImageSourceShouldCacheImmediately` true→false 일괄 치환
   - PhotoPreviewView.swift: line 409, 431, 2896, 2947, 2994
   - ThumbnailGridView.swift: line 2999, 3017, 3064, 4339
2. hiResCache cost 계산 수정 (PhotoPreviewView.swift:2774)
   - lazy decode 시 cgImage.bytesPerRow*height 직접 사용, fallback 10MB
3. PreviewImageCache 비활성화
   - maxBytes = 0 + set() early return (evict loop 차단)
   - reduceCacheLimit: 캐시 전체 클리어로 변경

### AI 캐시 누수 해소
4. EmbeddingIndex.topK 메모리 캐싱 (cachedAll + invalidateAllCache)
   - 폴더 전환/upsert/remove 시 무효화
5. VisualSearchService folderStates LRU (max 3개)
6. MemGuard Layer 3 에 AI 캐시 flush 추가
   - FaceEmbeddingCache.trimMemory() + EmbeddingIndex.close()
7. BurstPickerService 공유 CIContext (매 호출 신규 생성 제거)
8. SemanticSearchService indexing autoreleasepool 범위 확대 (skip 분기 포함)
9. MemGuard 임계값 하향: soft 8GB / warn 12GB / emerg 16GB (기존 26/39/42)

### 신규 기능
- 연사 베스트 자동 선별 (BurstDetectionService + BurstPickerService + BurstPickerDialog)
  · 시간 5초 + CLIP 유사도 기반 그룹화
  · 프리셋/체크박스/엄격도 슬라이더
- 사용자 학습 (SelectionEventStore SQLite + UserPreferenceProfile + PreferenceTrainingDialog)
  · 이벤트 원장 (rated/colorLabel/spacePick/gSelect/exported/uploaded/deleted/aiPick/aiReject)
  · 프로필 import/export + 3가지 병합 전략
  · 누적 통계 + 종류별 breakdown + DB 크기
- AdaFaceEmbeddingProvider (얼굴 검색 99.82%)
- VisualSearchMode.clothing (Person Segmentation + torso + CLIP)
- "All" 만능 탈출 버튼 (rating/color/minRating/showOnlySelected/visualSearch 모두 해제)
- 벌크 선택 이벤트 기록 (setRatingForSelected / setColorLabelForSelected / toggleSpacePickForSelected)

## 📊 실측
- 초기 RSS: 134MB
- 3분 스트레스 피크: 221MB
- MemGuard Layer 2/3 발동 없음
- 이전 18GB 대비 98.8% 감소

## 🔄 다음 세션 할 일

### Phase 3 — 렌더 루프 최적화
- [ ] PhotoStore: `invalidateFilterCache()` + `photosVersion += 1` 40+ 곳 통합
  - 방법: invalidateFilterCache 내부에서 photosVersion 증가, 호출부 라인 제거
- [ ] photoCount / spacePickCount 캐시화
  - `_cachedPhotoCount: Int?`, `_cachedSpacePickCount: Int?`
  - invalidateFilterCache 에서 nil 리셋
  - PhotoStore.swift:810
- [ ] resetFiltersIfEmpty 내부 filteredPhotos 중복 계산 완화
- [ ] ContentView 4~5개 싱글턴 @ObservedObject 재배치
  - UpdateService, MemoryCardBackupService, SubscriptionManager, GSelectService, ClientSelectService
  - 사용 하위 뷰로 이동해서 ContentView 전체 re-render 막기

### Phase 4 — Vision/BurstPicker 최적화
- [ ] VisualSearchService.evaluatePhoto 사진당 Vision request 5개 → 단일 handler 배치 (line 297~428)
- [ ] CacheSweeper aggressive 모드에서 previewCache 비활성 시 previews sweep skip (line 96)
- [ ] scrollMonitor 중복 등록 방지 (PhotoPreviewView.swift:1151)

### Phase 5 — 방어/통계
- [ ] SelectionEventStore.stats() → SQL COUNT 직접 (10만 경로 로드 제거)
- [ ] FaceEmbeddingCache 부분 LRU (현재 trimMemory 는 전체 비우기 — 항목 상한 2000 권장)
- [ ] NSImage size=pixel → point 기준 (PhotoPreviewView:414,440,481,526)
  - Retina 2x 백킹 버퍼 4배 메모리 우려 — 검증 필요
- [ ] PhotoItem struct 크기 축소 (rating/colorLabel/isSpacePicked 분리)

## 🗂 커밋/배포 규칙
- 마지막 커밋: **eb2c5d1** (v8.9.0 같은 옷 검색 + Visual Search 필터 복원)
- 현재 모든 수정 uncommitted
- **사용자가 "커밋해" 또는 "배포해" 명시할 때만** 커밋/푸시/DMG/공증/릴리스

## 📁 Dormant (미완성)
- CLIP 텍스트 검색 (`ai:` prefix)
  - similarity 0.08~0.12 비정상 (기대: 0.25~0.35)
  - 3개월+ 추가 작업 필요 판단 (2026-04-22 결정)
  - 파일은 존재: CLIPTokenizer.swift, TextEncoderService.swift, SemanticSearchService.searchByText
  - 조사 필요: BPE 토큰 ID, MobileCLIPText 모델 input shape/dtype, 모델 norm

## 🧪 주요 변경 파일 (uncommitted)

Models:
- PhotoStore.swift
- PhotoStore+Rating.swift

Services:
- MemoryGuardService.swift

Services/AI (신규):
- BurstDetectionService.swift
- BurstPickerService.swift
- SelectionEventStore.swift
- UserPreferenceProfile.swift
- CLIPTokenizer.swift (dormant)
- TextEncoderService.swift (dormant)

Services/AI (수정):
- EmbeddingIndex.swift
- VisualSearchService.swift
- FaceEmbeddingService.swift
- SemanticSearchService.swift

Views:
- PhotoPreviewView.swift
- ThumbnailGridView.swift
- ContentView.swift
- ContentView+Toolbar.swift
- VisualSearchHUD.swift
- VisualSearchCropView.swift

Views (신규):
- BurstPickerDialog.swift
- PreferenceTrainingDialog.swift

Resources:
- bpe_simple_vocab_16e6.txt.gz (1.3MB, CLIP 텍스트 검색용)

## 🚀 새 세션 재개 명령
```
/Users/potokan/PhotoRawManager/.claude-sessions/2026-04-22-v8.9-memory-handoff.md 읽고
Phase 3부터 이어서 진행
```
