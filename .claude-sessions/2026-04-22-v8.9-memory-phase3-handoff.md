# PickShot v8.9 메모리 최적화 Phase 3+ — 세션 핸드오프

**날짜**: 2026-04-22 (Phase 3+ 마무리)
**빌드**: BUILD SUCCEEDED
**앱 실행**: PID 97824, stderr → /tmp/pickshot_debug.log

## ✅ 이번 세션 적용

### Phase 3-1 — invalidateFilterCache 통합
중복 호출 9곳 정리 + 단독 photosVersion 6곳을 invalidateFilterCache로 교체.

수정 파일:
- `PhotoRawManager/Models/PhotoStore+Move.swift` (line 578, 615)
- `PhotoRawManager/Models/PhotoStore+Exif.swift` (line 97, 169)
- `PhotoRawManager/Models/PhotoStore+Analysis.swift` (line 85, 258)
- `PhotoRawManager/Views/ContentView.swift` (line 142, 376, 505)
- `PhotoRawManager/Views/MatchingView.swift` (4곳 → invalidateFilterCache)
- `PhotoRawManager/Views/MetadataEditorView.swift` (line 404)
- `PhotoRawManager/PhotoRawManagerApp.swift` (line 115)
- `PhotoRawManager/Services/PickshotFileService.swift` (line 285)
- `PhotoRawManager/Views/FileSyncPopupView.swift` (line 118)
- `PhotoRawManager/Views/ContentView+Toolbar.swift` (line 1322)

### Phase 3-3 — resetFiltersIfEmpty
`PhotoStore.swift:225` filter → `.lazy.filter` (중간 배열 제거)

### Phase 5-2 — FaceEmbeddingCache 부분 LRU
`Services/AI/FaceEmbeddingService.swift:432`
- `trimMemory(aggressive: Bool = false)` 추가
- 기본: 가장 오래된 절반만 evict (재계산 비용 절감)
- aggressive: 전체 비우기
- `MemoryGuardService.swift:183` Layer 3에서 `aggressive: true` 호출

## ✅ 이미 적용 확인 (Phase 1+2 작업)
- Phase 3-2 photoCount/spacePickCount 캐시 (PhotoStore.swift:809-811, refreshDerivedCountsIfNeeded)
- Phase 4-2 CacheSweeper preview sweep skip (CacheSweeper.swift:257-267)
- Phase 4-3 scrollMonitor 중복 가드 (PhotoPreviewView.swift:1153)
- Phase 5-1 SelectionEventStore.stats SQL COUNT (SelectionEventStore.swift:281)
- Phase 5-2 LRU 상한 maxMemoryEntries=2000 (FaceEmbeddingService.swift:363)

## 🔄 Deferred (다음 세션)

### Phase 3-4 — ContentView 5개 싱글턴 재배치
- UpdateService, MemoryCardBackupService, SubscriptionManager, GSelectService, ClientSelectService
- ContentView.swift line 6,7,8,15,16
- 위험: SwiftUI .sheet presentation context 분리 — 기존 동작 깨질 수 있음
- 접근: ProgressOverlayView, SheetMountView 별도 struct 추출

### Phase 4-1 — VisualSearchService Vision 단일 handler 배치
- VisualSearchService.swift:307 evaluatePhoto
- 사진당 4번 image decode (FaceEmbedding, BodyFeaturePrint, FeaturePrint, ClothingEmbedding)
- 단일 CGImage 공유 + VNImageRequestHandler.perform([req1,req2,req3]) 통합 필요
- 각 compute 함수 시그니처 (URL → CGImage) 리팩 필요

### Phase 5-3 — NSImage size point — **불필요 (검증 결과)**
NSImage(cgImage:size:)에서 size는 그리기 좌표계. cgImage 픽셀 버퍼 메모리와 무관.
메모리 영향 없음 — skip 결정.

### Phase 5-4 — PhotoItem struct 분리
- 25개 필드 → rating/colorLabel/isSpacePicked 분리
- 광범위 리팩 (61개 @Published 영향)
- 별도 세션 + 측정 선행 필요

## 🗂 git 상태
- Base commit: eb2c5d1 (v8.9.0)
- Phase 1+2+3 변경 모두 uncommitted
- **사용자 명시 시에만** 커밋/푸시/배포

## 🚀 새 세션 재개
```
/Users/potokan/PhotoRawManager/.claude-sessions/2026-04-22-v8.9-memory-phase3-handoff.md 읽고
Phase 3-4 또는 Phase 4-1 부터 진행
```
